(()=>{const D=window.COURSE_DATA||{};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const tag=s=>`<span class="tag">${esc(s)}</span>`;const card=(slug,c)=>`<a class="course-card" href="${slug}.html"><div class="course-card-top"><span class="course-status">${esc(c.status)}</span><span aria-hidden="true">→</span></div><h3>${esc(c.title)}</h3><p>${esc(c.summary)}</p><div class="course-card-meta">${esc(c.institution)}</div></a>`;function autoWeeks(c){const t=c.topics||[];const out=[];for(let i=0;i<14;i++){const x=t[Math.min(Math.floor(i/2),t.length-1)]||'Review';out.push([String(i+1),x+(i%2?' — applications and problem solving':' — concepts and worked examples'),'Practice / archive resource']);}return out;}function detail(){const file=location.pathname.split('/').pop()||'',slug=(document.body.dataset.course||file.replace(/\.html$/,'')),c=D[slug];if(!c)return;document.title=`${c.title} | Abdul Rab`;for(const [q,v] of [['[data-title]',c.title],['[data-status]',c.status],['[data-summary]',c.summary],['[data-inst]',c.institution],['[data-audience]',c.audience],['[data-summary2]',c.summary],['[data-assessment]',c.assessment||'Lecture notes, worked examples, practice sets, quizzes/assignments where available, and examination preparation.']]){const e=document.querySelector(q);if(e)e.textContent=v;}document.querySelector('[data-topics]').innerHTML=(c.topics||[]).map(tag).join('');const w=c.weeks||autoWeeks(c);document.querySelector('[data-weeks]').innerHTML=w.map(r=>`<tr><td>${esc(r[0])}</td><td>${esc(r[1])}</td><td>${esc(r[2])}</td></tr>`).join('');const nh=document.querySelector("[data-notes]");if(nh&&c.notes){nh.innerHTML=c.notes.map(n=>`<a class="note-card" href="${esc(n[2])}"><span class="note-section">${esc(n[0])}</span><strong>${esc(n[1])}</strong><span>Open web notes →</span></a>`).join("");}}function index(){const all=Object.entries(D),cur=all.filter(([,c])=>c.status.startsWith('Current')),arc=all.filter(([,c])=>!c.status.startsWith('Current'));document.querySelector('#current-courses').innerHTML=cur.map(([s,c])=>card(s,c)).join('');document.querySelector('#archive-courses').innerHTML=arc.map(([s,c])=>card(s,c)).join('');document.querySelector('[data-count]').textContent=all.length;}if(document.querySelector('[data-title]'))detail();else if(document.querySelector('#current-courses'))index();})();